apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-csi-noauth
  labels:
    app: nginx-csi-noauth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-csi-noauth
  template:
    metadata:
      labels:
        app: nginx-csi-noauth
    spec:
      containers:
      - image: nginx
        name: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: www
          mountPath: /usr/share/nginx/html
      volumes:
        - name: www
          persistentVolumeClaim:
            claimName: pvc-csi-noauth
